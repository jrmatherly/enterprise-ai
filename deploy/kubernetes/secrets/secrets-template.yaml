# ⚠️ TEMPLATE FILE - DO NOT COMMIT ACTUAL SECRETS ⚠️
#
# Copy this file to secrets.yaml and fill in real values.
# Add secrets.yaml to .gitignore!
#
# For production, use External Secrets Operator, Sealed Secrets, or similar.

---
# PostgreSQL credentials
apiVersion: v1
kind: Secret
metadata:
  name: postgresql-credentials
  namespace: eai
type: Opaque
stringData:
  password: "CHANGE_ME_postgres_password"

---
# Redis credentials
apiVersion: v1
kind: Secret
metadata:
  name: redis-credentials
  namespace: eai
type: Opaque
stringData:
  password: "CHANGE_ME_redis_password"

---
# Langfuse API credentials
apiVersion: v1
kind: Secret
metadata:
  name: langfuse-credentials
  namespace: eai
type: Opaque
stringData:
  publicKey: "pk-lf-CHANGE_ME"
  secretKey: "sk-lf-CHANGE_ME"

---
# Azure AI Foundry credentials
apiVersion: v1
kind: Secret
metadata:
  name: azure-ai-credentials
  namespace: eai
type: Opaque
stringData:
  eastusEndpoint: "https://CHANGE_ME.openai.azure.com/"
  eastusApiKey: "CHANGE_ME_api_key"
  eastus2Endpoint: "https://CHANGE_ME.openai.azure.com/"
  eastus2ApiKey: "CHANGE_ME_api_key"

---
# Azure EntraID (Azure AD) credentials
apiVersion: v1
kind: Secret
metadata:
  name: azure-entraid-credentials
  namespace: eai
type: Opaque
stringData:
  tenantId: "CHANGE_ME_tenant_id"
  clientId: "CHANGE_ME_client_id"
  clientSecret: "CHANGE_ME_client_secret"

---
# better-auth credentials (for frontend authentication)
apiVersion: v1
kind: Secret
metadata:
  name: better-auth-credentials
  namespace: eai
type: Opaque
stringData:
  # Generate with: openssl rand -base64 32
  secret: "CHANGE_ME_generate_random_32_byte_secret"
