apiVersion: v1
kind: Service
metadata:
  name: eai-frontend
  namespace: eai
  labels:
    app.kubernetes.io/name: enterprise-ai-platform
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: enterprise-ai-platform
    app.kubernetes.io/component: frontend
