# Enterprise AI Platform - Frontend Environment Variables
# Copy to frontend/.env.local for local development (outside Docker)
#
# NOTE: When running in Docker, these are set by docker-compose.yml
# This file is only needed for local development with `npm run dev`
#
# Full documentation: ../docs/setup/ENVIRONMENT-VARIABLES.md

# ============================================
# Better Auth
# ============================================
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=changeme-generate-with-openssl-rand-base64-32
BETTER_AUTH_URL=http://localhost:3001

# ============================================
# Database
# ============================================
# Same database as backend (better-auth stores sessions here)
DATABASE_URL=postgresql://postgres:your-password@localhost:5432/eai

# ============================================
# Microsoft Entra ID (SSO)
# ============================================
# Same values as backend - from Azure Portal > App Registrations
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
AZURE_CLIENT_SECRET=your-client-secret

# ============================================
# API Configuration
# ============================================
# Backend URL for API proxy route (server-side only)
BACKEND_URL=http://localhost:8000

# Public API URL for client-side calls (leave empty to use Next.js proxy)
NEXT_PUBLIC_API_URL=
